import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets(
    'refresh flow updates dashboard without blocking UI',
    (tester) async {
      // Placeholder: migrar a integration_test con backend/mock API controlado.
    },
    skip: true,
  );
}
